name: untitled2
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  # Poți lăsa versiunea SDK actuală dacă proiectul tău rulează
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # PAS 1: Adaugă pachetul GetX pentru State Management
  get: ^4.6.5
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # PAS 2: Declară explicit toate căile de assets
  assets:
    # Această linie este NECESARĂ pentru a încărca data.json
    - assets/
    # Această linie este corectă și necesară pentru imagini
    - assets/psycho/